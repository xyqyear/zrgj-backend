<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zrgj.order_everyday.mapper.AccountMapper">

    <select id="getAccountByUsername" parameterType="java.lang.String"
            resultType="zrgj.order_everyday.entity.Account">
        select *
        from account
        where username = #{username}
    </select>

    <insert id="addNewAccount" parameterType="zrgj.order_everyday.entity.Account">
        insert into account (username, position, password, avatar_url, restaurant_id)
        values (#{username}, #{position}, #{password}, #{avatarUrl}, #{restaurantId})
    </insert>

    <update id="updateAccount" parameterType="zrgj.order_everyday.entity.Account">
        update account
        set position=#{position},
            avatar_url=#{avatarUrl}
        where username = #{username}
    </update>

    <update id="changePassword" parameterType="zrgj.order_everyday.entity.Account">
        update account
        set password=#{password}
        where username = #{username}
    </update>

    <update id="deleteAccount" parameterType="java.lang.String">
        update account
        set deleted= true
        where username = #{username}
    </update>

    <select id="getAccountList" parameterType="java.lang.String"
            resultType="zrgj.order_everyday.entity.Account">
        select id, username, position, avatar_url, restaurant_id
        from account
        where restaurant_id = #{restaurantId}
          and position != 0
    </select>

</mapper>